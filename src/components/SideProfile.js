import { Box, Text } from "@chakra-ui/react";

const SideProfile = () => {
  return (
    <Box sx={{ bgColor: "green.300", width: "50%" }}>
      <Text>Hello</Text>
    </Box>
  );
};

export default SideProfile;
